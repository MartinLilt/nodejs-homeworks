const ctrlWrapper = require('./ctrlWrapper');
const validation = require('./validation');
const currAuthReq = require('./currAuthReq');
const upload = require('./upload');

module.exports = {
  ctrlWrapper,
  validation,
  currAuthReq,
  upload,
};
